/// <reference types="cypress" />
import { faker } from "@faker-js/faker";

describe("Funcionalide: Cadastro de livros", () => {
    beforeEach(() => {
        cy.visit('catalog.html');
    
    });
    it('Deve clicar no botão adicionar à cesta', () => {
        
    });
});